CREATE DATABASE loja;
USE loja;

CREATE TABLE produtos (
	PRODUTOS_ID INT PRIMARY KEY,
	COD_PROD INT NOT NULL ,
	VALOR_UNITE float NOT NULL,
	QUANTIDADE INT,
	DESCONTO INT
);


SELECT * FROM PRODUTOS; 
INSERT into PRODUTOS
VALUES 
(1,25.00,10,5),
(2,20.10,15,0);
